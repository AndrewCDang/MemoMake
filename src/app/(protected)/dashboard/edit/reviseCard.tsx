"use client";
import React from "react";
import Button from "@/app/_components/(buttons)/styledButton";

function ReviseCard() {
    return (
        <div>
            <Button variant="black" text="Revise Now" />
        </div>
    );
}

export default ReviseCard;
